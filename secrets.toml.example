# streamlit/secrets.toml (For local development in .streamlit/secrets.toml)
# Or paste these in the Streamlit Cloud dashboard under Settings -> Secrets

NEWS_API_KEY = "your_news_api_key"
ALPHA_VANTAGE_KEY = "your_alpha_vantage_key"
GROQ_API_KEY = "your_groq_api_key"

# The app automatically checks st.secrets first, then falls back to config.py
